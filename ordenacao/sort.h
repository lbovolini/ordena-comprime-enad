#ifndef SORT
#define SORT

#include <string.h>

// Insetionsort para strings
void insertionsort (char **, int *, int **, int, int);


// Quicksort para strings
void quicksort_string (char **, int *, int **, int, int, int, int);


// Quicksort para numeros
void quicksort_number (char **, int *, int **, int, int, int, int);


#endif